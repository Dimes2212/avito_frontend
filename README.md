# 🛒 Moderation Panel — Frontend Trainee Assignment (Autumn 2025)

Этот проект представляет собой интерфейс модерационной панели для проверки пользовательских объявлений.

Содержит:

- Главную страницу со списком объявлений
- Детальную страницу объявления
- Панель действий модератора
- Страницу статистики
- Фильтры, сортировки, пагинацию
- Графики активности и решений

Полностью соответствует техническому заданию.

## 🚀 Быстрый запуск

### Требования

- Node.js 18+
- npm 9+
- Backend API на порту **3001**

### Установка и запуск фронтенда

```bash
git clone https://github.com/Dimes2212/avito_frontend.git
cd avito_frontend/frontend
npm install
npm run dev
```

После запуска проект будет доступен по адресу:

➡️ **http://localhost:5173**

## 🖥 Запуск backend (API)

```bash
cd ../tech-int3-server
npm install
npm start
```

API запускается на:

➡️ **http://localhost:3001**

Фронтенд автоматически использует этот API.

## 🧱 Архитектура проекта

### Использованные технологии

React + TypeScript

React был выбран не «потому что модно», а потому что он позволяет максимально быстро собрать интерфейс без лишней бюрократии. Для задач уровня модерационной панели — списки, фильтры, страницы просмотра, таблицы, графики — React подходит идеально.
Он гибкий, предсказуемый и позволяет легко поддерживать проект в дальнейшем.

TypeScript — это просто спокойствие. При работе с реальными API (где структура данных может меняться, ломаться, расширяться) типы экономят огромное количество времени.
Когда ты знаешь, какая структура у объявления или статистики, пропадает 90% случайных ошибок.
Плюс — автодополнения, быстрая навигация, меньше багов.

Vite

Vite — это тот инструмент, который «не мешает работать». Он моментально запускается, не заставляет ждать HMR, не раздувает конфиги.
При разработке админки, где много правок в интерфейсе, это критично: изменения улетают в браузер за доли секунды.

MUI (Material UI)

Для панелей модерации дизайн обычно не является целью сам по себе — важны читаемость, контрастность, логичность интерфейсов.

MUI закрывает это из коробки:

карточки,

таблицы,

кнопки,

шапка,

контейнеры.

Можно сосредоточиться на логике, а не верстать кнопки и выпадашки по 20 минут.
Плюс он даёт аккуратный адаптив и матёрые UI-паттерны.

React Query (TanStack Query)

Для работы с API это почти идеальный инструмент.
Он берёт на себя всё, что обычно доводит до нервного тика:

загрузка,

ошибки,

кеширование,

повторные запросы,

синхронизация данных,

автоматическое обновление.

Проще говоря: ты пишешь меньше кода, а получаешь более надёжный интерфейс.
Redux здесь не нужен, потому что нет большого состояния внутри приложения — почти всё приходит с API.

Recharts

Recharts был выбран по одной простой причине: он решает задачу.
Никакого оверхеда, никакой боли — просто берёшь BarChart, PieChart, добавляешь легенду и значения, и всё работает.
Графики смотрятся аккуратно и читаемо, что важно для статистики модерации.

React Router

Страниц несколько, каждая со своей логикой (список → карточка → статистика), и нужен предсказуемый роутинг.
React Router отлично справляется:

динамические маршруты,

переходы между соседними объявлениями,

обработка ошибок 404.

Лёгкий, стабильный и не требует сложных настроек

Также использовался storybook для отсмотра готовых UI-компонентов

## 📄 Реализованный функционал

### ✔ Главная страница /list

- Список объявлений
- Фильтры (статус, категория, цена, поиск)
- Сортировки
- Пагинация

### ✔ Детальная страница /item/:id

- Галерея изображений
- Характеристики
- История модерации
- Решения модератора

### ✔ Страница статистики /stats

- Общая статистика
- Графики активности
- Графики решений
- Графики по категориям
